FROM redis:7

# Copy custom redis configuration if needed
# COPY redis.conf /usr/local/etc/redis/redis.conf

# Expose Redis port
EXPOSE 6379

# Use the default redis-server command with custom config
# CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]
CMD ["redis-server"]